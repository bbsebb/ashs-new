/**
 * Represents a membership category within a campaign.
 */
export interface Category {
  /** The name of the category. */
  name: string;
  /** The amount for this category. */
  amount: number;
}
